<div class="main-content">
  <div class="section__content section__content--p30">
    <div class="container-fluid">
      <div class="row">
        <div class="col-lg-12">
          <div class="row">
            <div class="col-md-12">
              <div class="overview-wrap">
                <h2 class="title-1">Shipping prices</h2>
                <%= link_to add_shipping_prices_path, class: "au-btn au-btn-icon au-btn--blue" do  %>
                  <i class="zmdi zmdi-plus"></i>Add Shipping price
                <% end %>
              </div>
            </div>
          </div>
          <br>
          <div class="table-responsive table--no-card m-b-30">
            <table class="table table-borderless table-striped table-earning" id="shipping_price_list">
              <thead>
              <tr>
                <th>ID</th>
                <th>name</th>
                <th class="text-right">Price</th>
                <th class="text-right">Created at</th>
              </tr>
              </thead>
              <tbody>
              <% @shipping_prices.each do |shipping| %>
                <tr>
                  <td><%= shipping.id %></td>
                  <td><%= shipping.name %></td>
                  <td><%= shipping.price %></td>
                  <td class="text-right"><%= shipping.created_at %></td>
                </tr>
              <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<script>
    $( document ).ready(function() {
        $("#shipping_price_list").dataTable();
    });
</script>