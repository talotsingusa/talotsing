development:
  adapter: redis
  url: redis://localhost:6379/1
test:
  adapter: redis
  url: <%= ENV["REDIS_URL"] %>
staging:
  adapter: redis
  url: <%= ENV["REDIS_URL"] %>
production:
  adapter: redis
  url: <%= ENV["REDIS_URL"] %>
